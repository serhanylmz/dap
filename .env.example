# --- LLM ---
ANTHROPIC_API_KEY=

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Admin / static-profile owner ---
# comma-separated emails with admin privileges (access to /admin).
# any email in this list signing in is also treated as owner of the
# STATIC_OWNER_HANDLE profile, so their auth user maps to that card.
ADMIN_EMAILS=
STATIC_OWNER_HANDLE=
